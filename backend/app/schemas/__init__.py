# Package schemas: chứa các Pydantic schemas dùng để validate dữ liệu đầu vào
# (request body) và định dạng dữ liệu đầu ra (response).
# Tách biệt hoàn toàn với Models để không lộ thông tin nhạy cảm (vd: password_hash).
