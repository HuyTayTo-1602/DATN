# =============================================================================
# requirements.txt
# Danh sách tất cả thư viện Python cần thiết cho backend.
# Cài đặt: pip install -r requirements.txt
# =============================================================================

# --- Framework chính ---
fastapi==0.115.0            # Web framework bất đồng bộ hiệu năng cao
uvicorn[standard]==0.30.6   # ASGI server để chạy FastAPI

# --- Database ---
sqlalchemy==2.0.35          # ORM để tương tác với PostgreSQL
psycopg2-binary==2.9.11     # Driver kết nối PostgreSQL
alembic==1.13.3             # Quản lý database migrations

# --- Xác thực & Bảo mật ---
passlib[bcrypt]==1.7.4      # Hash mật khẩu bằng bcrypt
python-jose[cryptography]==3.3.0  # Tạo và giải mã JWT token

# --- Validation & Cấu hình ---
pydantic==2.9.2             # Validate dữ liệu request/response
pydantic-settings==2.5.2    # Đọc cấu hình từ file .env
python-multipart==0.0.12    # Xử lý upload file (form-data)
email-validator==2.2.0      # Validate định dạng email trong Pydantic

# --- Tiện ích ---
python-dotenv==1.0.1        # Đọc file .env
httpx==0.27.2               # HTTP client bất đồng bộ (download CV từ URL, gọi LLM API)

# --- Chatbot AI (Sprint 1+) ---
langgraph==1.2.2            # LangGraph orchestration framework
langchain-core==1.4.0       # LangChain core abstractions (must be >=0.3.87 for langgraph 1.2.2)
langchain-anthropic==1.4.4  # Claude (Anthropic) LLM provider
langchain-groq==1.1.2       # Groq LLM provider (fallback when GROQ_API_KEY is set)
pypdf==6.12.2               # PDF text extraction

# --- File Storage (MinIO) ---
minio>=7.2.9                # MinIO Python SDK (S3-compatible object storage)

# --- PDF Parsing ---
PyMuPDF>=1.24.10            # PDF text extraction (import as fitz) — requires Python 3.13: use 1.27+

# --- PDF Generation (seed CV files) ---
fpdf2>=2.8.0               # Tạo file CV PDF từ text (render tiếng Việt qua font Unicode DejaVuSans)

# --- Testing ---
pytest==9.0.3               # Unit testing
pytest-asyncio==1.4.0       # Async test support
